# Context Index

Do not load context files by default. Open only the file that matches the task.

| Need | File |
|---|---|
| Repo layout / where to look | `repository-map.md` |
| Architecture / modules / source sets | `architecture.md` |
| Product terms / inventory semantics | `product.md` |
| Stack / tooling defaults | `stack.md` |

If repository code answers the task, skip these docs.
